
My dotfiles. To install:

    cd ~
    git clone git://github.com/learnjin/tildefiles.git
    mv tildefiles/.git .
    rm -rf tildefiles

And to overwrite your dotfiles with the ones fromt 
the repository:

    git reset --hard


